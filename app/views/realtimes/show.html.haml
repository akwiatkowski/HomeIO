%h1 Realtime

.container-fluid
  .row-fluid#data
    .span12
      %h2 Measurements

    - MeasType.all.each do |mt|
      .span3.meas_box{id: mt.name}
        = mt.name
        .meas_value= '...'


%script{:src => "http://localhost:8080/socket.io/socket.io.js"}
- if false
  :javascript
    var socket = io.connect('http://localhost:8080');
    socket.on('message', function (data) {
      console.log(data);
      $('#data').append(data);
      $('#data').append("<br>");
    });
- if true
  :javascript
    $(document).ready(function(){
      //realtime_init();
      realtime_put_value("batt_u", 1.0);
    });
