<%= form_for(@home_archive) do |f| %>
  <% if @home_archive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home_archive.errors.count, "error") %> prohibited this home_archive from being saved:</h2>

      <ul>
      <% @home_archive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %><br />
    <%= f.datetime_select :time %>
  </div>
  <div class="field">
    <%= f.label :home_archive_type %><br />
    <%= f.collection_select :home_archive_type_id, HomeArchiveType.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
